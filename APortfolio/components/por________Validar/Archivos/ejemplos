var hoja = document.createElement('style')
hoja.innerHTML = "div {border: 2px solid black; background-color: blue;}";
document.body.appendChild(hoja);


if (window.screen < 1162) {

var hoja = document.createElement('style');
hoja.innerHTML = "div {border: 2px solid black; background-color: blue;}";
document.body.appendChild(hoja);


onclick=""function_toggle(this)""

Me.lnk_Archivo.Attributes("onclick") = "javascript:popup_ventanaNuevo('-','' ,'' ,'' ,'&t=arc&tab=doc_documento_ot&id=" & Request.QueryString("idot") & "');return false;"
we war see


https://www.w3schools.com/css/tryit.asp?filename=trycss3_animation_count2






<body onresize="myFunction()">
<p id="demo"></p>
<script>
function myFunction() {
  var w = window.outerWidth;
  var h = window.outerHeight;
  var txt = "Window size: width=" + w + ", height=" + h;
  document.getElementById("demo").innerHTML = txt;
}
</script>
</body>

Dim array(2) As String
        array(0) = "Dog"
        array(1) = "Cat"
        array(2) = "Python"

        ' Join array.
        Dim result As String = String.Join(",", array)

        ' Display result.
        Console.WriteLine(result)




00000000000000000
generar registros de una tabla
    Desde SSMS, haz click derecho en la base de datos donde esta la tabla que necesitas.
    Selecciona Tasks (Tareas) -> Generate Scripts (Generar Scripts)
    Selecciona la tabla y avanza el Wizard a Set Scripting Options.
    En el menu Advanced (Avanzado) modifica "Types of data to script" de "Schema only" a "Data only"
    Click "Next" y "Finish" 










